name: Test ssh_keys_dir input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create ssh keys directory
        run: mkdir -p /tmp/test-ssh-keys

      - name: Run action with ssh_keys_dir input (expect failure due to no real GCP)
        id: action
        uses: .
        continue-on-error: true
        with:
          instance_name: fake-instance
          zone: us-central1-a
          user: testuser
          ssh_private_key: |
            -----BEGIN OPENSSH PRIVATE KEY-----
            b3BlbnNzaC1rZXktdjEAAAAA
            -----END OPENSSH PRIVATE KEY-----
          command: echo hello
          project_id: fake-project-12345
          ssh_keys_dir: /tmp/test-ssh-keys

      - name: Assert action failed (not skipped) with ssh_keys_dir set
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "skipped" ]; then
            echo "FAIL: action was skipped unexpectedly"
            exit 1
          fi
          if [ "$OUTCOME" = "success" ]; then
            echo "FAIL: expected action to fail with fake credentials but it succeeded"
            exit 1
          fi
          echo "PASS: action correctly failed (not skipped) when ssh_keys_dir is set"
